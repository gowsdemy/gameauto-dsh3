# ============================================================
#  GameMale 自动签到 · 配置文件
#  用法：复制本文件为 config.env，填好下面各项即可（最少填 USERNAME / PASSWORD）。
#  ⚠️ config.env 含你的账号密码，请勿分享或上传给别人。
# ============================================================

# 【必填】你的论坛用户名（登录名）
USERNAME=
# 【必填】你的论坛登录密码
PASSWORD=

# 【选填】发件邮箱的 SMTP 服务器地址，按你的邮箱类型挑一个填（对照表见文末）
SMTP_HOST=
# 【选填】发件邮箱的完整地址，例如 123456@qq.com
MAIL_USER=
# 【选填】SMTP 授权码（不是邮箱登录密码，获取方法见文末）
MAIL_PASS=
# 【选填】收件邮箱；留空则默认发给上面的发件邮箱（即发给自己）
MAIL_TO=

# 【选填】每天自动运行的时间，24 小时制 HH:MM，例如 00:10；留空默认 00:10
AUTOTIME=

# 【选填】手动指定浏览器完整路径；留空则自动检测 Edge / Chrome
#BROWSER_PATH=


# ============================================================
#  补充说明（下面是详细解释，改上面的变量时可以参考）
# ============================================================

# 1) 邮件通知（选填）
#    想每天把签到结果发到你邮箱：填好 SMTP_HOST / MAIL_USER / MAIL_PASS 三项即可；
#    不想收邮件：这三项（含 MAIL_TO）全部留空，脚本会自动跳过发信，不影响签到。

# 2) 常用邮箱 SMTP 服务器地址对照表（把地址填到 SMTP_HOST）：
#      QQ 邮箱        ->  smtp.qq.com
#      网易 163 邮箱  ->  smtp.163.com
#      网易 126 邮箱  ->  smtp.126.com
#      新浪邮箱       ->  smtp.sina.com
#      Foxmail        ->  smtp.foxmail.com
#      Gmail          ->  smtp.gmail.com
#      阿里云邮箱     ->  smtp.aliyun.com

# 3) SMTP 授权码怎么拿（填到 MAIL_PASS，注意不是邮箱登录密码）：
#      - QQ 邮箱：网页版 → 设置 → 账号与安全 → POP3/IMAP/SMTP → 开启 SMTP 服务 → 生成授权码
#      - 163 邮箱：网页版 → 设置 → POP3/SMTP/IMAP → 开启 SMTP 服务 → 新增授权密码

# 4) 每天自动运行时间（AUTOTIME）：
#      填好之后，双击「配置计划任务.bat」会按这个时间创建"每天自动运行"的计划任务；
#      留空则默认 00:10。

# 5) 浏览器（BROWSER_PATH）：
#      脚本会自动检测 Edge / Chrome，一般不用填；
#      若没检测到、或想强制用某个浏览器，去掉行首的 # 并填完整路径，例如：
#      BROWSER_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
