# GameMale 自动签到 · Cloudflare 拦截诊断与修复说明

## 一、现象

`gowsdemy/gameauto` 的 GitHub Actions 每天定时跑 `gamemale.py`：
- 最后一次**完全正常**是 2026-09-04（run #148）。
- 从 2026-09-05 起，每次运行表面显示"成功"，但实际上**什么都没做**——脚本在第一步登录时就失败，日志只有一句：
  `验证码识别失败，中止登录`。
- 而且 `gold_record.txt` 不再更新（没有新的自动提交）。

## 二、根本原因（已在真实的 GitHub Actions 服务器上实测确认）

论坛 `www.gamemale.com` 新增了 **Cloudflare「请进行人机验证」门**（由 Discuz 插件 `dev8133_cloudflare` 提供，
用的是 Cloudflare Turnstile，站点 key `0x4AAAAAAEqRGyPbvEznAcKy`）。

验证结果（来自 GitHub Actions 的 Ubuntu 运行环境，数据中心 IP）：
- 对首页、登录页、验证码接口、论坛页 **所有请求**都会返回这个"请稍候..."人机验证页。
- 用普通 `requests`、`curl_cffi`（伪装 Chrome 指纹）、`playwright`+stealth、`patchright`（无头/有头）**全部过不了**，
  等 95 秒以上仍拿不到验证令牌。

**关键结论：** 这不是脚本代码坏了，而是该站点的 Cloudflare 针对"数据中心 IP"（GitHub Actions、云服务器等）强制**交互式人机验证**。
只有"家庭宽带 / 手机流量"这类 Cloudflare 信任的 IP，Turnstile 才会**自动通过**（非交互式）。

## 三、修复方案

因为核心矛盾是"流量来源 IP 不被 Cloudflare 信任"，只有两条路：

| 方案 | 说明 | 成本 |
| --- | --- | --- |
| **A. GitHub Actions + 住宅代理** | 在云端继续定时跑，但通过住宅代理发起请求 | 需购买住宅代理（约几十元/月） |
| **B. 在自己电脑上跑** | 用家庭 IP 直接跑，无需代理 | 免费，但运行时间电脑需开机 |

本仓库代码**同时支持 A 和 B**（`gamemale_v2.py` 已实现 Turnstile 解门 + 可选代理 + 原有全部功能）。

## 四、本仓库文件说明

| 文件 | 作用 |
| --- | --- |
| `gamemale_v2.py` | **修复后的主脚本**：先判断是否被 Turnstile 拦截；若是，用真实浏览器（Playwright Chromium）自动通过人机验证并取得通行 Cookie，再交给原有逻辑完成签到/抽奖/互动/资产/邮件。支持通过 `PROXY_URL` 或 `PROXY_*` 环境变量配代理。 |
| `run_local.bat` | Windows 一键运行脚本（方案 B 用）：自动装环境、下载浏览器并运行。 |
| `config.env.example` | 本地配置模板，复制为 `config.env` 并填入账号密码。 |
| `gamemale.py` | 原脚本（保留，供对照）。 |
| `.github/workflows/signin_v2.yml` | 方案 A 用的云端工作流：装了 Playwright，支持代理变量。 |
| `README_本地运行.md` | 方案 B 的详细本地安装 + Windows 任务计划程序设置。 |
| `gold_record.txt` | 每日金币对比基准。 |

## 五、需要你注意

- **方案 B 关键点**：你电脑所在网络的 IP 必须能被 Cloudflare 认为可信（普通家庭/手机宽带即可）。
  如果你手动用浏览器打开 `https://www.gamemale.com/` 也要做"人机验证"，说明你的网络也不可信，那就得改用方案 A（代理）。
- `config.env` 含真实账号密码，属于敏感信息，**不要提交到公开仓库**（本仓库是私有仓库，且已 gitignore）。
- 运行依赖：Windows + Python 3.10+，或装有 Python 的环境。`run_local.bat` 会自动安装 `requests`、`ddddocr`、`playwright`。

## 六、后续

- 若你在本地用 `run_local.bat` 跑通（能收到邮件、`gold_record.txt` 更新），即可把本仓库的 `gamemale_v2.py` 同步到主仓库 `gameauto`。
- 若遇到报错（过不了人机验证、装依赖失败、网络不通等），把报错信息发给我，我继续帮你调。
