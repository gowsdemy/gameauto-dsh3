# GameMale 自动签到 · 分发版使用说明

一个用于 GameMale 论坛的**纯本地自动化**脚本：每天自动帮你完成登录、签到、抽奖、互动，
并把当天的金币等资产变化保存；**可选**通过邮件把结果发给你。

> 为什么是本地运行：论坛现在加了 Cloudflare「请进行人机验证」，会拦截云服务器/数据中心 IP
> （如 GitHub Actions）。因此用**你电脑上的真实 Edge 浏览器**运行，才能通过验证。
> 需要 Windows + Python 3.10+ + 已安装 Microsoft Edge。

---

## 一、快速开始（下载即用）

1. 把本压缩包解压到一个文件夹（路径别带中文和空格，建议如 `C:\gameauto`）。
2. 进入该文件夹，把 `config.env.example` **复制一份**，重命名为 `config.env`。
3. 用记事本打开 `config.env`，**至少填两项必填**：
   ```
   USERNAME=你的论坛用户名
   PASSWORD=你的论坛登录密码
   ```
   （邮件是**可选的**：`SMTP_HOST`/`MAIL_USER`/`MAIL_PASS` 都留空就跳过发信，其余任务照常完成。）
4. 双击运行 `run_local.bat`。
   - 首次运行会自动创建 Python 环境并安装依赖（`ddddocr`、`playwright`），较慢，请耐心。
   - 之后会**弹出真实 Edge 窗口**加载论坛；若 Cloudflare 验证未自动通过，**点一下窗口里的验证框**即可。
   - 看到「所有作业同步执行完毕」即成功；结果也会打印在窗口里（以及可选发到你的邮箱）。

## 二、邮箱通知（可选）
在 `config.env` 里填写邮箱相关项即可自动发邮件：
```
SMTP_HOST=smtp.qq.com        # 用你发件邮箱对应的服务器地址（qq/163/gmail 等）
MAIL_USER=你的发件邮箱@qq.com
MAIL_PASS=你的SMTP授权码      # 不是邮箱登录密码！在邮箱设置→开启SMTP后生成授权码
MAIL_TO=                     # 收件邮箱；留空默认发给发件人自己
```
若这三项留空，脚本会**跳过邮件**，不影响其它任务。

## 三、每天自动运行（推荐）
用 Windows「任务计划程序」定时运行 `run_local.bat`：
1. 按 Win 键，搜索并打开「任务计划程序」。
2. 右侧点「创建任务」→ 名称填 `GameMale 签到`。
3. 「触发器」→ 新建 → 设置你想要的每天时间（建议各设一个早上和一个晚上，例如 00:20 和 14:40）。
4. 「操作」→ 新建 → 程序/脚本填 `cmd`，添加参数填：
   `/c "C:\gameauto\run_local.bat"`（把 `C:\gameauto` 换成你的实际路径）。
5. 「条件」页取消勾选「只有在计算机使用交流电源时才启动」。
6. 确定保存。之后每天到点会自动运行。

## 四、常见问题
- **弹出 Edge 窗口并要求点验证框**：这是 Cloudflare 的正常验证，点一下即可；有时也会自动通过。
- **提示没有 config.env**：把 `config.env.example` 复制为 `config.env` 并填好。
- **收不到邮件**：先看 `config.env` 是否填了 `SMTP_HOST`/`MAIL_USER`/`MAIL_PASS`（邮箱留空则不发送）；若填了，去垃圾箱看看，并把发件人加白名单。
- **金币记录**：脚本会在同目录生成 `gold_record.txt` 保存每日金币，用于次日对比。

## 五、安全提醒
- `config.env` 含你的**真实账号密码**，**不要**把它分享/上传给别人，也**不要**提交到公开仓库。
- 本压缩包内的 `config.env.example` 是空的模板，不含任何个人信息。

---
GameMale 自动签到 · 分发版
